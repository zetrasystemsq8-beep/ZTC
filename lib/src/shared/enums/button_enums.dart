enum ButtonVariant {
  primary,
  secondary,
  outline,
  ghost,
  danger,
  success,
}

enum ButtonSize {
  small,
  medium,
  large,
}

enum ButtonState {
  idle,
  loading,
  disabled,
  success,
  error,
}
